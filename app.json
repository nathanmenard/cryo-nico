{
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "standard-1x"
        }
      },
      "env": {
        "TZ": {
          "value": "Europe/Paris"
        },
        "DATABASE_CLEANER_ALLOW_PRODUCTION": {
          "value": "false"
        },
        "DATABASE_CLEANER_ALLOW_REMOTE_DATABASE_URL": {
          "value": "true"
        }
      },
      "addons":[
        "heroku-postgresql"
      ],
      "scripts": {
        "test": "bundle exec rspec"
      }
    }
  }
}
